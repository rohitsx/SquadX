generator client {
  provider = "prisma-client-js"
  output   = "./generated/client2"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO_URL")
}

model User {
  id       Int      @id @map("_id")
  username String
  image    String
  
  @@map("users")
}
